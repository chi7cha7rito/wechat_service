<%- include("../common/head") %>
    <h1 class="name">
        <%= matchName %>
    </h1>
    <div class="wrapper">
        <% if (rewards) { %>
            <% rewards.forEach(function(item){ %>
                <div class="weui-form-preview">
                    <div class="weui-form-preview__bd">
                        <div class="weui-form-preview__item">
                            <label class="weui-form-preview__label">名次</label>
                            <span class="weui-form-preview__value">第<%= item.ranking %>名</span>
                        </div>
                        <div class="weui-form-preview__item">
                            <label class="weui-form-preview__label">积分奖励</label>
                            <span class="weui-form-preview__value"><%= item.rewardPoints %> 积分</span>
                        </div>
                        <% if(item.remark && item.remark.length) {%>
                            <div class="weui-form-preview__item">
                                <label class="weui-form-preview__label">奖励说明</label>
                                <span class="weui-form-preview__value"><%= item.remark %></span>
                            </div>
                        <%}%>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
                <div class="weui-loadmore weui-loadmore_line">
                <span class="weui-loadmore__tips">暂无奖励</span>
                </div>
        <% } %>
    </div>
<%- include("../common/footer") %>


